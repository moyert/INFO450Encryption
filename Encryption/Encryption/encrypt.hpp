#include <iostream>
#include <fstream>
#include <vector>
#include <string>
using namespace std;

class encryt
{
    char ch;
    string line;
public:
    int encrypt(char ch);
    int decrypt(char ch);
};
